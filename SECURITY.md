# Security

Do not commit real bank exports, credentials, cache files, logs, screenshots with personal data, or generated analysis based on personal transactions.

Use `.env` for local secrets. Keep only `.env.example` in version control.

If you find a secret or personal data sample in this public copy, treat it as a publication blocker.
